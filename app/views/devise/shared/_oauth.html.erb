<%- if devise_mapping.omniauthable? %>
  <%- resource_class.omniauth_providers.each do |provider| %>
    <%= link_to "Sign in with #{OmniAuth::Utils.camelize(provider)}", "/auth/#{provider.to_s}", class: "btn-auth btn-auth--#{provider}" %>
  <% end -%>
<% end -%>